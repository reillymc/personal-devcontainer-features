{
    "name": "Worktrees",
    "id": "worktrees",
    "version": "0.0.3",
    "description": "Mounts the git worktrees root dir into the container",
    "mounts": [
        {
            "source": "$GIT_REPO",
            "target": "$GIT_REPO",
            "type": "bind"
        }
    ]
}
