{
    "name": "Worktrees",
    "id": "worktrees",
    "version": "0.0.1",
    "description": "Mounts the git worktrees root dir into the container",
    "onCreateCommand": "sh create.sh",
    "mounts": [
        {
            "source": "$GIT_REPO",
            "target": "$GIT_REPO",
            "type": "bind"
        }
    ]
}
